@model IEnumerable<PROJ_SW.Model.Cart>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.User.FName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.User.FName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.cart_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.cart_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.cart_id })
        </td>
    </tr>
}

</table>
